# --- Discord Connection ---

DISCORD_TOKEN=your-bot-token-here

# Main channel where new trade alerts are posted.
DISCORD_CHANNEL_TRADES_ID=your-alerts-channel-id

# Channel where WIN/LOSS notifications get posted when a trade closes.
# Separate from the main alert channel so closed-trade results don't get
# lost among new signals.
DISCORD_CHANNEL_TRADES_HISTORY_ID=your-closed-trades-channel-id

# Channel where the end-of-session retrospective is posted on weekdays
# (Mon–Fri at SESSION_END_HOUR:15 Berlin time). Leave blank to fall back
# to DISCORD_CHANNEL_TRADES_HISTORY_ID.
DISCORD_CHANNEL_RETROSPECTIVE_ID=your-retrospective-channel-id

# Channel for non-tier-A alerts. Empty = everything goes to the main alerts
# channel (no change).
DISCORD_CHANNEL_FIREHOSE_ID=

# --- Scanning & Session ---

# Trading session window (Europe/Berlin time, DST-aware). The bot scans
# continuously during this window instead of just once a day.
SESSION_START_HOUR=8
SESSION_END_HOUR=23

# How often to scan during the session, in minutes. Every scan both looks
# for new trades AND checks all open trades for near-close proximity.
SCAN_INTERVAL_MINUTES=5

# A signal must appear the same way on this many CONSECUTIVE scans before
# it's confirmed and alerted -- filters flicker from today's still-forming
# daily candle. Higher = fewer false alarms, but slower to alert.
SIGNAL_CONFIRMATION_SCANS=2

# After a plan closes, how long before the SAME
# ticker/strategy/horizon/direction may alert again. Before this shipped the
# answer was "never": confirm_or_update() stamped the setup as confirmed and
# nothing ever cleared it, so a level that kept holding still only ever
# produced one plan. 5 matches the backtest's cooldown_bars -- the spacing the
# measured win rate was actually produced under. 0 re-arms as soon as the plan
# closes; either way the setup must clear every filter and re-serve the full
# SIGNAL_CONFIRMATION_SCANS debounce before it can post again.
SIGNAL_REENTRY_COOLDOWN_DAYS=5

# DEBUG shows per-horizon scanning detail and full confidence breakdowns
# for every scenario checked, not just the ones that alert.
LOG_LEVEL=INFO

# --- Trade Filters & Risk ---

# Hard filter, enforced exactly as set: a scenario is dropped entirely
# (not shown, not scored) unless its target sits at least this much
# away from TODAY'S CURRENT PRICE. No exceptions for a close miss.
#
# NOTE (2026-08-06): this flat value is largely inert in the live scan.
# engine.py applies max(MIN_REWARD_PCT, horizon's sr_target_min_pct * 0.15),
# and the per-horizon term dominates for nearly every horizon -- lowering
# this from 3.0 to 2.0 moved the scan funnel by exactly zero scenarios.
# It still binds on the shortest horizons, and it is the denominator of the
# confidence score's target-distance component (confidence.py), so it is not
# a no-op -- just not the lever it looks like.
MIN_REWARD_PCT=2.0

# Hard filter: dropped entirely if the stop sits closer than this to
# entry -- too exposed to ordinary daily noise. No exceptions.
MIN_STOP_DISTANCE_PCT=2.0

# Hard filter: dropped entirely if the stop sits further than this from
# entry. Keep this in the 5–7% range for disciplined position sizing.
MAX_STOP_LOSS_PCT=7.0

# Hard filter: dropped entirely unless the reward:risk to target 1
# clears this ratio. No exceptions for a close miss.
MIN_RISK_REWARD_RATIO=1.5

# How close (as a %) an independent strategy's own predicted level has
# to land to a scenario's actual target/stop price to count as
# confirming it (see MIN_TARGET_CONFLUENCE_COUNT below).
CONFLUENCE_DEVIATION_PCT=5.0

# A scenario is flagged unless at least this many DISTINCT strategies
# land within CONFLUENCE_DEVIATION_PCT of the target price.
# 1 disables this requirement.
MIN_TARGET_CONFLUENCE_COUNT=2

# Only Level 4 (High) and above auto-alert. Lower levels are still
# computed and shown via !check but won't post automatically.
MIN_ALERT_CONFIDENCE_LEVEL=4

# Two signals on the same ticker/direction with entry, SL, and TP all
# within this % of each other get merged into one alert.
DEDUP_TOLERANCE_PCT=2.0

# Post a warning when an open trade's price gets within NEAR_CLOSE_THRESHOLD_PCT
# of its SL/TP. Set to false to temporarily silence these without losing the
# threshold below -- SL/TP hits themselves are unaffected either way.
NEAR_CLOSE_ALERTS_ENABLED=false

# How close (in %) the current price must get to a trade's stop-loss or
# take-profit before a near-close warning posts to
# DISCORD_CHANNEL_TRADES_HISTORY_ID. Fires once per approach.
NEAR_CLOSE_THRESHOLD_PCT=2.0

# If a trade reaches NEAR_TP_TIMEOUT_THRESHOLD_PCT of the way to its target
# (as a % of the entry -> target 1 distance) and then sits there without
# actually touching the target for NEAR_TP_TIMEOUT_MINUTES, it's closed early
# at the live price to lock in the profit already made instead of risking it
# waiting for the exact target price. Dropping back below the threshold
# resets the clock. Set NEAR_TP_TIMEOUT_ENABLED=false to disable entirely.
#
# A faster exit than the full timeout: once price is in the near-TP zone,
# NEAR_TP_STALL_CHECK_MINUTES/_MAX_FLUCTUATION_PCT close the trade early if
# price hasn't moved more than the fluctuation % within that shorter window
# (must be less than NEAR_TP_TIMEOUT_MINUTES).
NEAR_TP_TIMEOUT_ENABLED=true
NEAR_TP_TIMEOUT_THRESHOLD_PCT=80.0
NEAR_TP_TIMEOUT_MINUTES=10
NEAR_TP_STALL_CHECK_MINUTES=5
NEAR_TP_STALL_MAX_FLUCTUATION_PCT=0.5

# How many past trading days the daily retrospective remembers, so it can
# notice repeating problems (e.g. "3rd day in a row") instead of treating
# every day in isolation with no memory of what was said yesterday.
RETROSPECTIVE_HISTORY_DAYS=60

# --- Multi-Timeframe Confluence ---

# Check each ticker's higher-timeframe EMA bias before accepting a signal.
# Counter-trend signals are penalised by HTF_COUNTER_TREND_PENALTY points.
HTF_CONFLUENCE_ENABLED=true

# Raw confidence score points subtracted when a signal goes against its
# ticker's own higher-timeframe EMA trend. 15 drops a borderline Lv3
# signal to Lv2 and below the default MIN_ALERT_CONFIDENCE_LEVEL gate.
HTF_COUNTER_TREND_PENALTY=15

# --- Data & Display ---

# How much daily history to pull for live scanning.
DEFAULT_HISTORY_PERIOD=10y

# Fallback currency symbol when a ticker's real currency can't be detected.
CURRENCY_SYMBOL=€

# Market regime benchmark ticker (used to gauge overall market trend).
MARKET_REGIME_TICKER=SPY

# --- Account Defaults ---

# Editable at runtime via !account, stored in account.json after first run.
ACCOUNT_BALANCE=1000000
RISK_PER_TRADE_PCT=1.0
MAX_OPEN_POSITIONS=30
MAX_POSITION_SIZE_PCT=5.0

# How each trade's position size is calculated:
#   risk_pct    -- fixed-fractional (original model): size so a full stop-out
#                  costs exactly RISK_PER_TRADE_PCT% of the account.
#   account_pct -- fixed allocation: the position itself is always exactly
#                  POSITION_SIZE_PCT_OF_ACCOUNT% of the account balance,
#                  regardless of stop distance (e.g. 0.1% of a €1,000,000
#                  account opens a €1,000 position every time).
# Both are editable at runtime via !account sizing / !account positionpct.
POSITION_SIZING_MODE=risk_pct
POSITION_SIZE_PCT_OF_ACCOUNT=0.101

# Hard caps applied on top of whichever sizing mode is active, in currency
# units -- whichever binds first wins. They exist so a wide stop or a large
# account can never open a position bigger than intended.
MAX_POSITION_VALUE_ABSOLUTE=1000
MAX_RISK_AMOUNT_ABSOLUTE=100

# Portfolio-level exposure caps, applied across all open plans at once
# (the per-trade caps above only ever see one trade). Heat = summed open
# risk as a % of balance: PORTFOLIO is the total ceiling, CORRELATED
# bounds a cluster of names that move together, SECTOR bounds one sector.
PORTFOLIO_TARGET_DAILY_VOL_PCT=0.7
PORTFOLIO_HEAT_CAP_PCT=6.0
CORRELATED_HEAT_CAP_PCT=3.0
SECTOR_HEAT_CAP_PCT=3.0

# Cap on simultaneously open plans in any single horizon, so one horizon
# can't quietly consume the whole book.
MAX_OPEN_PER_HORIZON=4

# Scale risk down while the account is in drawdown.
DD_THROTTLE_ENABLED=false

# --- Secondary Alerts (optional) ---

# Minimum confidence level for the cheaper secondary channels (email/push).
# Independent of MIN_ALERT_CONFIDENCE_LEVEL, which governs Discord.
SECONDARY_ALERT_MIN_CONFIDENCE=4

# Send email alerts via SMTP in addition to Discord.
ALERT_EMAIL_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
# Email address the alerts are sent FROM. For Gmail use an App Password
# (not your account password). Renamed from SMTP_USERNAME -- that spelling
# was never read by the config parser.
SMTP_USER=
SMTP_PASSWORD=
ALERT_EMAIL_TO=

# Send push notifications via ntfy.sh (free, no account needed).
ALERT_PUSH_ENABLED=false
NTFY_TOPIC=

# --- Plan Engine v2 rollout (see README "Plan Engine v2" section) ---
# Defaults ship fully live (on/true/true) for immediate deployment. Set
# PLAN_ENGINE_V2=shadow first instead if you'd rather compare v2 plans
# against legacy numbers for a few scan sessions before trusting them live.

# off = legacy alerts | shadow = v2 plans logged to data/shadow_plans.jsonl
# but not posted | on = alerts price and emit v2 plans (badges, TP1/TP2).
PLAN_ENGINE_V2=on
# At TP1, close 50% and move the stop to break-even; the runner trails
# toward TP2. Hardcoded on whenever the intraday manager below runs -- there
# is no flag for this (SCALE_OUT_ENABLED used to exist here but was deleted
# in plan v8 Task V4: plan_manager.py never actually read it).
# 60s monitor drives the plan lifecycle (fills, BE moves, partials, trail)
# with a Discord alert per transition; !plans shows the live board.
INTRADAY_MANAGER_V2=true
# Floor under TP1 as a % of entry: no plan may aim for a win smaller than
# this, whatever the reward:risk math produces. TARGET_FLOOR_ENABLED=false
# computes and logs the floor without applying it, so a week of scans can
# measure how many setups would survive it before targets actually move.
#
# ROLLED BACK TO false 2026-08-06 (human-partner call on V29's fired rollback
# trigger). MIN_TARGET_PCT is left at 2.5 deliberately: with the floor off it
# is inert, and keeping the value means re-enabling is a one-flag change back
# to the exact geometry that was measured, not a re-guess of the number.
MIN_TARGET_PCT=2.5
TARGET_FLOOR_ENABLED=false

# Hard ceiling on the stop distance: no plan may risk more than this % of
# entry, whatever the per-strategy stop produces (ATR multiple, fib/wave
# structure, or the S/R fixed percent). Counterpart to MIN_TARGET_PCT --
# that sets the win side, this bounds the loss side, and together they set
# the payoff ratio. Applied BEFORE the target is derived, so capping the
# stop never silently changes R:R. Position size is computed off the
# capped distance, so risk per trade does not move.
#
# At 2.5%/1.5% the payoff is 1.67R and break-even is 37.5%, against a
# measured ~46% win rate -- a live margin of ~8.5 points. (Plan v8 V51
# shipped 1.75%, i.e. 1.43R / 41.2% break-even; 1.5% widens the margin.)
# MAX_LOSS_CAP_ENABLED=false ignores the cap and leaves stops at their
# per-strategy distance, for measuring what it would have changed.
#
# ROLLED BACK TO false 2026-08-06 alongside TARGET_FLOOR_ENABLED. The two set
# the payoff ratio jointly, so rolling back one and leaving the other on would
# produce a geometry that was never measured in either configuration.
MAX_LOSS_PCT=1.5
MAX_LOSS_CAP_ENABLED=false

# --- Early exits (V51 Step 3) -- all three ship OFF -------------------
# Pre-registered predicates for V52 to grid ONE AT A TIME, not shipped
# behavior: each changes what a loss costs and none has been measured.
# Kept as three independent flags so their effects stay separable.
#
# THESIS -- cut when the setup's premise is broken.
EARLY_CUT_THESIS_ENABLED=false
# TIME   -- cut after N bars if the trade hasn't earned MIN_R by then.
EARLY_CUT_TIME_ENABLED=false
EARLY_CUT_TIME_BARS=5
EARLY_CUT_TIME_MIN_R=0.5
# MAE    -- cut once adverse excursion reaches FRACTION of the stop,
#           but only if the trade never ran further than MAX_MFE_R first.
EARLY_CUT_MAE_ENABLED=false
EARLY_CUT_MAE_FRACTION=0.75
EARLY_CUT_MAE_MAX_MFE_R=0.25
# Post alerts that carry no v2 plan (no source/tier/badge). Off by default
# since plan v8 Task V13: that untiered cohort ran 154 live trades at 26.0%
# WR for -103.0%, ~72% of all damage. Leaving this false while
# PLAN_ENGINE_V2 is off/shadow suppresses ALL alerts (every alert is legacy
# in those modes) -- the scan logs a warning when that pairing is set.
LEGACY_ALERT_PATH_ENABLED=false

# --- Discord Alerts ---

# detailed = full trade-plan embed | compact = condensed one-screen card.
ALERT_EMBED_LAYOUT=detailed

# Post one end-of-day digest of the top plans instead of relying only on
# per-alert posts. DIGEST_MAX_PLANS caps how many it lists.
DAILY_DIGEST_ENABLED=false
DIGEST_MAX_PLANS=3

# --- Execution Realism ---

# Frictions applied to backtests and paper fills so measured expectancy
# isn't flattered by free, instant execution. SLIPPAGE_BPS is basis points
# off the fill; COMMISSION_PER_TRADE is currency per trade; the risk basis
# is the per-trade risk ($) that commission is expressed against when
# converting it into R.
SLIPPAGE_BPS=5
COMMISSION_PER_TRADE=1.0
COMMISSION_RISK_BASIS=100.0

# --- Universe & Scanning ---

# Which tickers get scanned:
#   watchlist | sp500 | sp500_top150 | etfs | sp500+etfs
SCAN_UNIVERSE=watchlist

# Liquidity floors applied to the universe before scanning.
UNIVERSE_MIN_DOLLAR_VOL=20000000
UNIVERSE_MIN_PRICE=5.0

# Skip a ticker within this many days of its earnings date. 0 = off (the
# Gatekeeper's own GATE_EARNINGS_BLACKOUT_DAYS is the enforced one).
EARNINGS_BLACKOUT_DAYS=0

# Parallel workers for a scan pass, and the ceiling on how many alerts a
# single scan may post.
SCAN_WORKERS=4
MAX_ALERTS_PER_SCAN=10

# Background OHLCV cache refresh.
MARKET_DATA_AUTO_REFRESH=true
MARKET_DATA_REFRESH_MINUTES=60
MARKET_DATA_TIMEFRAMES=monthly,weekly,daily,hourly

# Optional analysis layers -- all ship OFF; each adds work per scan and
# none is required for the shipped setup pipeline.
REGIME_GATES_ENABLED=false
AVWAP_LEVELS_ENABLED=false
PYRAMIDING_ENABLED=false
VOLUME_PROFILE_NODES_ENABLED=false
DECISION_CHART_ENABLED=false
DATA_DRIVEN_STOPS_ENABLED=false

# --- Gatekeeper (plan v8 Task V14) ---
# Scores each candidate on the checklist and tiers it C/B/A/A+.
# GATE_MODE=enforce actually blocks below GATE_MIN_TIER; `shadow` scores and
# logs without blocking. GATE_BLOCK_ACTION is left at its `flag` default per
# V15: a blocked alert still posts, headlined ENTRY BLOCKED with size 0, and
# no paper trade is logged.
#
# GATE_MIN_TIER selects which tiers PASS -- it does not change scoring, so
# raising it cannot create better candidates and lowering it cannot create
# A+ ones. Live evidence 2026-08-06: C=14, B=1, A=1, A+=0 across 16 gate
# evaluations, best score ever 81.33 against GATE_TIER_APLUS_CUT=90.
#
# ROLLED BACK TO false 2026-08-06 (human-partner call on V29's fired rollback
# trigger). V14's enforce mode was one of three changes in flight during the
# window where live-polled expectancy read -0.333R / 46.43% WR against a
# -0.082R / 68.58% baseline, and V29 explicitly could not attribute the drop
# to any one of them. GATE_MODE/GATE_MIN_TIER are left at their values because
# GATE_ENABLED=false means no gate code runs anywhere -- they are inert until
# the master switch returns, and preserving them keeps the re-test one flag.
GATE_ENABLED=false
GATE_MODE=enforce
GATE_MIN_TIER=C

# flag = a blocked alert still posts, headlined ENTRY BLOCKED with size 0
# and no paper trade logged | suppress = it isn't posted at all.
GATE_BLOCK_ACTION=flag

# Preset bundle of the scoring thresholds: strict | balanced | relaxed.
GATE_STRICTNESS=balanced

# Show gate reasoning on alerts even while GATE_MODE=shadow.
GATE_SHOW_IN_SHADOW=false

# Tier score cuts. A candidate scoring >= APLUS is A+, >= A is A, and so
# on; below B_CUT it is C. See the live-evidence note above -- best score
# observed to date is 81.33, so APLUS at 90 has never yet been reached.
GATE_TIER_APLUS_CUT=90.0
GATE_TIER_A_CUT=75.0
GATE_TIER_B_CUT=55.0

# Gate's own liquidity floor, independent of UNIVERSE_MIN_DOLLAR_VOL.
GATE_MIN_DOLLAR_VOL=2000000

# Event blackouts. ENABLED computes and reports them; ENFORCE is what
# actually blocks -- both must be true for a blackout to stop an alert.
# The hour windows bracket a scheduled macro release.
GATE_BLACKOUT_ENABLED=false
GATE_BLACKOUT_ENFORCE=false
GATE_BLACKOUT_HOURS_BEFORE=18
GATE_BLACKOUT_HOURS_AFTER=2
GATE_EARNINGS_BLACKOUT_DAYS=3

# Macro context (VIX, economic releases) via FRED. Empty FRED_API_KEY =
# FRED-backed data degrades to 'unknown'; scanning continues either way.
MACRO_ENABLED=false
MACRO_SNAPSHOT_TTL_MIN=30
# Free key: https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=
# Free key: https://finnhub.io/register -- powers the earnings calendar.
# Empty = earnings proximity degrades to 'unknown'.
FINNHUB_API_KEY=

# --- Data Sources (optional) ---

# API key for Financial Modeling Prep (https://financialmodelingprep.com),
# used by the FMP crawler (scripts/fmp_crawl.py) to pull prices, fundamentals,
# earnings, transcripts, etc. Works on the free tier (gated endpoints are
# skipped) and any paid tier. Leave blank to disable FMP crawling.
FMP_API_KEY=

# --- Admin UI ---

# Protects the admin web UI with HTTP Basic Auth. CHANGE THESE.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
# Port the admin UI listens on (docker-compose.yml maps this to the host).
ADMIN_PORT=1234
DASHBOARD_REFRESH_SECONDS=10
LOGS_REFRESH_SECONDS=10
