import { provideZonelessChangeDetection } from '@angular/core'; import { TestBed } from '@angular/core/testing'; import { beforeEach,describe,expect,it } from 'vitest'; import { InlineMd,inlineSegments } from './inline-md';
describe('inlineSegments',()=>{it('splits bold',()=>expect(inlineSegments('Stop at **425.76** (-3.3%)')).toEqual([{text:'Stop at ',bold:false},{text:'425.76',bold:true},{text:' (-3.3%)',bold:false}]));it('keeps unclosed markup literal',()=>expect(inlineSegments('2 * 3 and **open')).toEqual([{text:'2 * 3 and **open',bold:false}]));});
describe('InlineMd',()=>{beforeEach(()=>TestBed.configureTestingModule({providers:[provideZonelessChangeDetection()]}));it('uses text interpolation rather than HTML',()=>{const f=TestBed.createComponent(InlineMd);f.componentRef.setInput('text','<img src=x> **ok**');f.detectChanges();expect(f.nativeElement.querySelector('img')).toBeNull();expect(f.nativeElement.querySelector('b').textContent).toBe('ok');});});
