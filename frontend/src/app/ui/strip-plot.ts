import {ChangeDetectionStrategy,Component,computed,input,signal} from '@angular/core';import {CHART_CHROME} from './chart/chart-frame';import {ChartTooltip,HoverState,hoverPosition} from './chart-tooltip';
export interface StripGroup {label:string;values:readonly number[];tone?:'pos'|'neg'|'accent'} export function median(values:readonly number[]):number|null{if(!values.length)return null;const s=[...values].sort((a,b)=>a-b),m=s.length>>1;return s.length%2?s[m]:(s[m-1]+s[m])/2} const W=320,ROW=34,LABEL_W=110,PAD=8;
@Component({selector:'sb-strip-plot',changeDetection:ChangeDetectionStrategy.OnPush,imports:[ChartTooltip],template:`<div class="host" #host><svg [attr.viewBox]="'0 0 '+w+' '+height()" width="100%" [attr.height]="height()" role="img" aria-label="Distribution by group">@for(g of rows();track g.label;let i=$index){<text class="label" [attr.x]="labelW-6" [attr.y]="i*rowH+14" text-anchor="end">{{g.label}}</text><text class="sub" [attr.x]="labelW-6" [attr.y]="i*rowH+27" text-anchor="end">N={{g.values.length}}@if(g.median!==null){ · median {{format()(g.median)}}{{unit()}}}</text><line class="base" [attr.x1]="labelW" [attr.x2]="w-pad" [attr.y1]="i*rowH+20" [attr.y2]="i*rowH+20"/>@for(v of g.values;track $index;let j=$index){<line class="tick" [attr.data-tone]="g.tone??'accent'" [attr.x1]="x(v)" [attr.x2]="x(v)" [attr.y1]="i*rowH+12+jitter(j)" [attr.y2]="i*rowH+28+jitter(j)" tabindex="0" (pointermove)="show($event,g.label,v,host)" (focus)="show($event,g.label,v,host)" (pointerleave)="hover.set(null)" (blur)="hover.set(null)"/>}@if(g.median!==null){<line class="median" [attr.x1]="x(g.median)" [attr.x2]="x(g.median)" [attr.y1]="i*rowH+6" [attr.y2]="i*rowH+34"/>}}</svg><sb-chart-tooltip [state]="hover()" [hostWidth]="host.clientWidth"/></div>`,styles:`.host{position:relative}svg{display:block}.label{fill:var(--text);font-size:11px}.sub{fill:var(${CHART_CHROME.tickColour});font-size:10px}.base{stroke:var(${CHART_CHROME.grid});stroke-width:1}.tick{stroke-width:2;opacity:.7}.tick[data-tone='pos']{stroke:var(--pos)}.tick[data-tone='neg']{stroke:var(--neg)}.tick[data-tone='accent']{stroke:var(--accent)}.median{stroke:var(--text);stroke-width:2}`}) export class StripPlot{readonly groups=input.required<readonly StripGroup[]>();readonly unit=input('d');readonly format=input<(v:number)=>string>((v)=>v.toFixed(2));protected readonly w=W;protected readonly rowH=ROW;protected readonly labelW=LABEL_W;protected readonly pad=PAD;protected readonly hover=signal<HoverState|null>(null);protected readonly height=computed(()=>Math.max(1,this.groups().length)*ROW+6);private readonly max=computed(()=>Math.max(.01,...this.groups().flatMap(g=>g.values)));protected x(v:number){return LABEL_W+v/this.max()*(W-LABEL_W-PAD)}protected jitter(j:number){return(j*7)%9-4}protected readonly rows=computed(()=>this.groups().map(g=>({...g,median:median(g.values)})));protected show(e:PointerEvent|FocusEvent,g:string,v:number,h:HTMLElement){this.hover.set({...hoverPosition(e,h),title:g,rows:[{label:'held',value:`${this.format()(v)}${this.unit()}`}]})}}
