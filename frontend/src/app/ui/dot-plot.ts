import { ChangeDetectionStrategy, Component, computed, input, signal } from '@angular/core';
import { CHART_CHROME } from './chart/chart-frame'; import { ChartTooltip, HoverState, hoverPosition } from './chart-tooltip';
export interface DotPoint { label: string; n: number; value: number | null; }
const W=320,H=180,PAD={l:36,r:12,t:8,b:22};
@Component({selector:'sb-dot-plot',changeDetection:ChangeDetectionStrategy.OnPush,imports:[ChartTooltip],template:`<div class="host" #host><svg [attr.viewBox]="'0 0 '+w+' '+h" width="100%" [attr.height]="h" role="img" [attr.aria-label]="yLabel()+' against sample size'"><line class="axis" [attr.x1]="pad.l" [attr.x2]="w-pad.r" [attr.y1]="y(0)" [attr.y2]="y(0)"/><line class="floor" [attr.x1]="x(floor())" [attr.x2]="x(floor())" [attr.y1]="pad.t" [attr.y2]="h-pad.b"/>@for(p of points();track p.label){<circle class="dot" [class.withheld]="p.value===null" [attr.cx]="x(p.n)" [attr.cy]="y(p.value??0)" r="4"/><circle class="hit" [attr.cx]="x(p.n)" [attr.cy]="y(p.value??0)" r="12" tabindex="0" (pointermove)="show($event,p,host)" (focus)="show($event,p,host)" (pointerleave)="hover.set(null)" (blur)="hover.set(null)"/>}</svg><table class="sr-only"><caption>{{yLabel()}} by sample size</caption>@for(p of points();track p.label){<tr><th scope="row">{{p.label}}</th><td>N={{p.n}}</td><td>{{p.value===null?'withheld (below floor)':format()(p.value)}}</td></tr>}</table><sb-chart-tooltip [state]="hover()" [hostWidth]="host.clientWidth"/></div>`,styles:`.host{position:relative}svg{display:block}.axis{stroke:var(${CHART_CHROME.axis});stroke-width:1}.floor{stroke:var(--text-faint);stroke-width:1;stroke-dasharray:2 3}.dot{fill:var(--accent)}.dot.withheld{fill:none;stroke:var(--text-faint);stroke-width:1.5}.hit{fill:transparent;outline:none}.sr-only{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0 0 0 0)}`})
export class DotPlot { readonly points=input.required<readonly DotPoint[]>();readonly floor=input.required<number>();readonly yLabel=input('ExpR');readonly format=input<(v:number)=>string>((v)=>v.toFixed(2));protected readonly w=W;protected readonly h=H;protected readonly pad=PAD;protected readonly hover=signal<HoverState|null>(null);private readonly xMax=computed(()=>Math.max(this.floor()*2,...this.points().map(p=>p.n),10));private readonly yAbs=computed(()=>Math.max(.5,...this.points().map(p=>Math.abs(p.value??0))));protected x(n:number){return PAD.l+Math.log10(Math.max(1,n))/Math.log10(this.xMax())*(W-PAD.l-PAD.r)}protected y(v:number){const a=this.yAbs();return PAD.t+(a-v)/(2*a)*(H-PAD.t-PAD.b)}protected show(e:PointerEvent|FocusEvent,p:DotPoint,host:HTMLElement){this.hover.set({...hoverPosition(e,host),title:p.label,rows:[{label:this.yLabel(),value:p.value===null?'withheld':this.format()(p.value)},{label:'trades',value:String(p.n)}]})}}
