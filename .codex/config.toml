# Project-scoped Codex permissions. This layer applies after the repository is
# marked trusted in Codex.
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[windows]
sandbox = "elevated"
[agents]
max_concurrent_threads_per_session = 1
