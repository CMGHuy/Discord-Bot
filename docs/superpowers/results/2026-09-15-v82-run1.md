# v82 earnings blackout — Run 1 (2018-06-01..2023-12-31)

## Replay

`complete: 13332 rows`

89/89 cached tickers produced a shard (`data/v82/run1/*.jsonl`); re-running
`replay --run run1` is a no-op against the existing shards, confirming the
run finished cleanly with a stable `calendar.json` (no exit-4 metadata
mismatch).

## Coverage

`python scripts/backtest/measure_earnings_blackout.py coverage --run run1 --window 2018-06-01..2023-12-31`

Exit code: `0` (coverage >= `COVERAGE_FLOOR_PCT` for the full Run 1 window).

Per the plan, an exit code of `2` here would not have been a stop — M10
applies the floor to the `SELECTION_WINDOW` specifically. It passed anyway.
